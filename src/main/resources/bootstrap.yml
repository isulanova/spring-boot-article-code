spring:
  application:
    name: ${SERVICE_NAME:nl-role-service}
  cloud:
    config:
      uri: ${CONFIG_URL:https://nl-dev.ru.auchan.com/config}
      fail-fast: true
  profiles:
    active: ${PROFILE:dev}
